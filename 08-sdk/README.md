# 08 - SDK

Goal: generate and use the extensible SDK for application development.

- Follow `sdk-generation.md` to build an eSDK and install it locally.
- Capture any host-toolchain conflicts and fixes in your journal.
