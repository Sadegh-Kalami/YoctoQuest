# 02 - Build System

Goal: set up poky, create a clean build directory, and run a first image build while understanding where files land.

- Follow `poky-setup.md` to clone poky and initialize the build environment.
- Use `first-build.md` to drive `bitbake core-image-minimal` and capture timings.
- Review `build-directory-structure.md` so you know what to ignore and where logs live.
