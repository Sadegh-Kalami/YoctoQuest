# 04 - Layers

Goal: understand layer structure, dependencies, and how to create and register your own layer cleanly.

- Read `understanding-layers.md` for conventions and priorities.
- Use `creating-layers.md` to create `meta-yoctoquest` and add it to `BBLAYERS`.
- Store working examples under `layer-examples/`.
