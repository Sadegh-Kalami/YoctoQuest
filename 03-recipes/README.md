# 03 - Recipes

Goal: learn recipe syntax, create your own, and study examples.

- Use `recipe-syntax.md` as a quick reference for common variables and tasks.
- Follow `writing-recipes.md` to build a small example recipe.
- Drop working samples in `examples/` (keep them minimal and documented).
